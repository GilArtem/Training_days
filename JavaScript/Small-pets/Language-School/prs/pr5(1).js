// исходные данные
const repeatCounter = 4;

// инициализация стартового значения
let index = 1;

// проверка условия
if (index <= repeatCounter) {
    document.write('к');
}

// инициализация стартового значения
index = 2;

// проверка условия
if (index <= repeatCounter) {
    document.write('к');
}

// инициализация стартового значения
index = 3;

// проверка условия
if (index <= repeatCounter) {
    document.write('к');
}

// инициализация стартового значения
index = 4;

// проверка условия
if (index <= repeatCounter) {
    document.write('к');
}

// инициализация стартового значения
index = 5;

// проверка условия
if (index <= repeatCounter) {
    document.write('к');
}

// инициализация стартового значения
index = 6;

// проверка условия
if (index <= repeatCounter) {
    document.write('к');
}

// инициализация стартового значения
index = 7;

// проверка условия
if (index <= repeatCounter) {
    document.write('к');
}